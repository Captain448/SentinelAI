# Docker Deployment Configuration

This folder is set aside for storing staging, production, or custom docker configurations.
To run the primary container architecture, run the `docker-compose.yml` file located at the root of the project:

```bash
docker compose up --build
```
